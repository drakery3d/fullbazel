export class Config {
  // TODO idea: generate interface for secrets and configs to have intellisense for config value keys
  // then we would do identifier: keyof configMap
  get(identifier: string) {}
}
